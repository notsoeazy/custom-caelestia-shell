import qs.components.effects
import qs.services
import qs.config
import qs.utils

ColouredIcon {
    source: SysInfo.osLogo
    implicitSize: Appearance.font.size.large * 1.7
    colour: Colours.palette.m3primary
}
